# LIF-NN

This repository contains a single script implementing a spiking neural network trained on MNIST using STDP-based learning rules.

## Usage

Install the required dependencies, then run the script:

```bash
python stdp_bm.py
```

The script will download the MNIST dataset and start training immediately.
